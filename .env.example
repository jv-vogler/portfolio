# =============================================================================
# Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values.
#
#   cp .env.example .env.local
#
# Never commit .env.local — it is git-ignored.
# =============================================================================

# ---------------------------------------------------------------------------
# Site URL (used for metadata, sitemap, OG images)
# ---------------------------------------------------------------------------
# Production: your Vercel production domain
# Development: leave empty to default to https://jvogler.vercel.app
NEXT_PUBLIC_BASE_URL=

# ---------------------------------------------------------------------------
# Resend (contact form email delivery)
# ---------------------------------------------------------------------------
# API key from https://resend.com/api-keys
RESEND_API_KEY=

# Recipient email address for contact form submissions
CONTACT_EMAIL_TO=

# Sender email address
# Development: use onboarding@resend.dev (Resend sandbox)
# Production: use a verified domain address, e.g. contact@yourdomain.com
CONTACT_EMAIL_FROM=onboarding@resend.dev

# ---------------------------------------------------------------------------
# Google Analytics 4 (optional)
# ---------------------------------------------------------------------------
# GA4 measurement ID from https://analytics.google.com
# Only active in production — omit or leave empty to disable.
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# ---------------------------------------------------------------------------
# PayloadCMS (Content Management System)
# ---------------------------------------------------------------------------
# Secret key for encrypting PayloadCMS cookies and JWT tokens
# Generate a secure random string (32+ characters)
# Example: openssl rand -base64 32
PAYLOAD_SECRET=

# PostgreSQL database connection string
# Auto-provided by Vercel Neon Postgres integration
# Run `vercel env pull` to sync these from your Vercel project
# POSTGRES_URL=
# POSTGRES_USER=
# POSTGRES_PASSWORD=
# POSTGRES_HOST=
# POSTGRES_DATABASE=
# NEON_PROJECT_ID=

# Additional Postgres variables (auto-generated by Vercel, optional)
# DATABASE_URL=
# DATABASE_URL_UNPOOLED=
# POSTGRES_URL_NON_POOLING=
# POSTGRES_URL_NO_SSL=
# POSTGRES_PRISMA_URL=
# PGHOST=
# PGHOST_UNPOOLED=
# PGUSER=
# PGPASSWORD=
# PGDATABASE=

BLOB_READ_WRITE_TOKEN=
